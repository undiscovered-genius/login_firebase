package com.example.login;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.widget.Button;
import android.widget.EditText;

import com.google.firebase.FirebaseApp;

public class Details extends AppCompatActivity {

    //EditText namedata, iddata, messagedata;
    //Button send, details;
    //FirebaseApp firebase;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_details);


            //namedata = findViewById(R.id.namedata);
            //iddata = findViewById(R.id.iddata);
            //messagedata = findViewById(R.id.messagedata);
            //send = findViewById(R.id.btn_send);
            //details = findViewById(R.id.btn_details);
            //firebase.getApplicationContext();

    }
}
